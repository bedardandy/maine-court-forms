{
  "form_id": "GS-018",
  "generated_by": "tools/derive_logic.py",
  "note": "If/then fill logic (authored + derived): conditional-required, attachment/companion-form triggers, value incompatibilities, and value inferences. Warnings-only — evaluated by tools/logic_engine.py at preflight/fill time; nothing here blocks or alters a fill. Not legal advice.",
  "rules": [
    {
      "id": "attach-immediate_income_withholding_order_attached_and_incorporated_herein",
      "kind": "attachment",
      "severity": "info",
      "when": {
        "truthy": "field:immediate_income_withholding_order_attached_and_incorporated_herein"
      },
      "then": {
        "attachment": "Immediate income withholding order"
      },
      "message": "You indicated an attachment ('Immediate income withholding order') — make sure the document is actually attached to the filing."
    }
  ]
}
