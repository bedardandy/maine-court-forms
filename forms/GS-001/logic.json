{
  "form_id": "GS-001",
  "generated_by": "tools/derive_logic.py",
  "note": "If/then fill logic (authored + derived): conditional-required, attachment/companion-form triggers, value incompatibilities, and value inferences. Warnings-only — evaluated by tools/logic_engine.py at preflight/fill time; nothing here blocks or alters a fill. Not legal advice.",
  "rules": [
    {
      "id": "attach-yes_consent_attached",
      "kind": "attachment",
      "severity": "info",
      "when": {
        "truthy": "field:yes_consent_attached"
      },
      "then": {
        "attachment": "Yes consent"
      },
      "message": "You indicated an attachment ('Yes consent') — make sure the document is actually attached to the filing."
    },
    {
      "id": "attach-yes_consent_attached_2",
      "kind": "attachment",
      "severity": "info",
      "when": {
        "truthy": "field:yes_consent_attached_2"
      },
      "then": {
        "attachment": "Yes consent"
      },
      "message": "You indicated an attachment ('Yes consent') — make sure the document is actually attached to the filing."
    },
    {
      "id": "attach-yes_consent_attached_3",
      "kind": "attachment",
      "severity": "info",
      "when": {
        "truthy": "field:yes_consent_attached_3"
      },
      "then": {
        "attachment": "Yes consent"
      },
      "message": "You indicated an attachment ('Yes consent') — make sure the document is actually attached to the filing."
    }
  ]
}
