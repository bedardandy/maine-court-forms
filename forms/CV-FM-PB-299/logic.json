{
  "form_id": "CV-FM-PB-299",
  "generated_by": "tools/derive_logic.py",
  "note": "If/then fill logic (authored + derived): conditional-required, attachment/companion-form triggers, value incompatibilities, and value inferences. Warnings-only — evaluated by tools/logic_engine.py at preflight/fill time; nothing here blocks or alters a fill. Not legal advice.",
  "rules": [
    {
      "id": "attach-in_01_increments_you_must_attach_itemization_of_time",
      "kind": "attachment",
      "severity": "info",
      "when": {
        "truthy": "facts.total_hours"
      },
      "then": {
        "attachment": "In 01 increments you must attach itemization of time"
      },
      "message": "You indicated an attachment ('In 01 increments you must attach itemization of time') — make sure the document is actually attached to the filing."
    }
  ]
}
