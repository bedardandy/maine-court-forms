{
  "form_id": "FM-070",
  "generated_by": "tools/derive_logic.py",
  "note": "If/then fill logic (authored + derived): conditional-required, attachment/companion-form triggers, value incompatibilities, and value inferences. Warnings-only — evaluated by tools/logic_engine.py at preflight/fill time; nothing here blocks or alters a fill. Not legal advice.",
  "rules": [
    {
      "id": "attach-attach_an_additional_piece_of_paper_signed_under_oath_if_necessary_1",
      "kind": "attachment",
      "severity": "info",
      "when": {
        "truthy": "facts.violation_details"
      },
      "then": {
        "attachment": "Attach an additional piece of paper signed under oath if necessary 1"
      },
      "message": "You indicated an attachment ('Attach an additional piece of paper signed under oath if necessary 1') — make sure the document is actually attached to the filing."
    }
  ]
}
