{
  "form_id": "CV-266",
  "generated_by": "tools/derive_logic.py",
  "note": "If/then fill logic (authored + derived): conditional-required, attachment/companion-form triggers, value incompatibilities, and value inferences. Warnings-only — evaluated by tools/logic_engine.py at preflight/fill time; nothing here blocks or alters a fill. Not legal advice.",
  "rules": [
    {
      "id": "wf-companion-CV-001",
      "kind": "companion_form",
      "severity": "info",
      "when": null,
      "then": {
        "form": "CV-001",
        "required": true
      },
      "message": "Filing CV-266 requires companion form CV-001."
    },
    {
      "id": "attach-two_copies_of_the_foreign_judgment_to_be_registered_are_attached_i_certif",
      "kind": "attachment",
      "severity": "info",
      "when": {
        "truthy": "field:two_copies_of_the_foreign_judgment_to_be_registered_are_attached_i_certify_that_the_attached_foreign"
      },
      "then": {
        "attachment": "Two copies of the foreign judgment to be registered are"
      },
      "message": "You indicated an attachment ('Two copies of the foreign judgment to be registered are') — make sure the document is actually attached to the filing."
    }
  ]
}
