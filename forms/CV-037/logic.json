{
  "form_id": "CV-037",
  "generated_by": "tools/derive_logic.py",
  "note": "If/then fill logic (authored + derived): conditional-required, attachment/companion-form triggers, value incompatibilities, and value inferences. Warnings-only — evaluated by tools/logic_engine.py at preflight/fill time; nothing here blocks or alters a fill. Not legal advice.",
  "rules": [
    {
      "id": "wf-companion-CV-001",
      "kind": "companion_form",
      "severity": "info",
      "when": null,
      "then": {
        "form": "CV-001",
        "required": true
      },
      "message": "Filing CV-037 requires companion form CV-001."
    },
    {
      "id": "time-pm-at_time",
      "kind": "value_inference",
      "severity": "info",
      "when": {
        "matches": [
          "field:at_time",
          "^(0?[1-6]):[0-5]\\d$"
        ]
      },
      "then": {
        "suggest": "A court time like this without AM/PM is almost always PM — confirm 'At time' is PM, not AM."
      },
      "message": "'At time' looks like an early-hour time with no AM/PM; court times are almost always PM — confirm."
    }
  ]
}
