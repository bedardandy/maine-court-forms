# Runtime dependencies for the form-by-form library.
#
# Core fill path (engine/) + scaffold tool (tools/):
maine-forms-engine @ git+https://github.com/bedardandy/maine-forms-engine@v0.3.0
PyMuPDF>=1.24      # fitz — read AcroForm schema + write/flatten filled PDFs
PyYAML>=6.0        # form.yaml / metadata
pikepdf>=8.0       # shared-AcroForm-field split guard (via maine-forms-engine;
                   # stops mapped values fanning out to a field's other
                   # appearances, e.g. OTH-029 `2_5`); fills skip the split
                   # (with a warning + result flag) if it is missing

# Agent MCP server (tools/mcp_server.py — find_forms / get_form / fill_form):
mcp[cli]>=1.0      # only needed to run the MCP server; the CLIs work without it

# Optional — only for the vision audit half of engine/fill_and_audit.py,
# which also needs a local vision-language model endpoint (see engine/README.md).
# Pillow>=10.0
# Optional — only for tools/vision_audit.py (OpenAI-compatible VL client):
# openai>=1.0
